<%= form_for(tech_tree) do |f| %>
  <% if tech_tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tech_tree.errors.count, "error") %> prohibited this tech_tree from being saved:</h2>

      <ul>
      <% tech_tree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :master_tech_id %>
    <%= f.text_field :master_tech_id %>
  </div>

  <div class="field">
    <%= f.label :master_tech %>
    <%= f.text_field :master_tech %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
